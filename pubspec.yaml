name: corona_patients_number
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.5"

dependencies:
  flutter:
    sdk: flutter

  flutter_localization: ^0.1.10

  flutter_riverpod: ^1.0.3
  http: ^0.13.5
  html: ^0.15.1
  loading_animation_widget: ^1.2.0+3
  liquid_progress_indicator: ^0.4.0
  flutter_launcher_icons: ^0.11.0
  get_it: ^7.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.3.2
  build_runner: ^2.3.3
  flutter_lints: ^2.0.1


flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path_ios: 'images/ios_icon.png'
  image_path_android: 'images/android_icon.png'
  adaptive_icon_background: '#e1f9f8'
  adaptive_icon_foreground: 'images/android_icon.png'

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
flutter_intl:
  enabled: true
